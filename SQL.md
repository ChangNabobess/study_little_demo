**存储过程**
是一组完成特定功能的 SQL 语句集，经编译后存储在数据库中，用户通过指定存储过程的名字和参数来调用它（oracle-Pl/SQL、SqlServer/T-SQL）；
**存储引擎**
数据在硬盘上怎么存储，数据库底层负责存取数据、索引数据、以及更新、查询数据的核心组件(InnoDb、MyISAM)；
**伸缩性和并行性**
数据库在处理海量数据卷（用户增长、数据量扩大）的能力，以及多处理器环境下同时执行任务的效率；
**HINT**
数据库优化器的提示，用于优化查询性能；
**关系型数据库**
是一种基于关系模型来组织、管理数据的系统，使用行列形式存储数据，并且用结构化 SQL 查询；
**ODBC**
使用一种开放数据库连接的标准接口，它允许不同的应用程序以统一的方式链接并访问数据库；
**JDBC**
它是专门为 Java 语言 设计的数据库连接标准,Java 程序或 JSP 网页可以通过 JDBC 驱动程序与 Oracle、MySQL 等数据库建立连接并交换数据
**OCI**
这是 Oracle 专用的底层网络客户连接接口。它是 Oracle 支持的多种工业标准连接方式之一，通常用于需要高性能、深度集成 Oracle 特性的应用程序
**Windows NT**
是微软公司开发的一系列操作系统（现在 Windows Server 的前身）。SQL Server 最初就是为该系统开发的。而 Oracle 同样支持在该平台上运行，且在 Windows NT 上的操作方式与在 Unix 下基本相同
**UNIX**
这是一种历史悠久、以高可靠性和伸缩性著称的操作系统,Oracle 在 Unix 平台上拥有极其丰富的开发经验和极高的集成度，在处理大数据量的关键业务时，Unix 平台通常被认为比早期的 Windows 平台更具优势
**sql 查询语句顺序问题**
正常查询数据返回使用的是数据在表中的自然顺序，但是如果当前数据执行过修改、编辑、删除等操作，那么这个顺序将会是受到 DBMS 重用回收存储空间的方式影响；
**sql 中子句的定义**
SQL 语句由子句（order By）构成，有些子句是必需的，有些则是可选的。一个子句通常由一个关键字加上所提供的数据组成。子句的例子有我们在前一课看到的 SELECT 语句的 FROM 子句

```sql
select * from table_name order by id;
```
